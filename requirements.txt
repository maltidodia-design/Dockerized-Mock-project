# Runtime dependencies only (what gunicorn/the Docker image needs).
# Test tooling lives in requirements-dev.txt so the production image stays
# minimal and pip-audit's gate operates on the real production surface.
Flask>=2.2
Flask_SQLAlchemy>=3.0
gunicorn>=20.1
